import { LatLng, ParkCodes } from "../../model";
export interface CalculateMarkerPositionArgs {
    position?: LatLng;
    parkCode: ParkCodes;
    sectorCode: string;
}
export interface ExtractSectorCenterLocationArgs {
    parkCode: ParkCodes;
    sectorCode: string;
}
