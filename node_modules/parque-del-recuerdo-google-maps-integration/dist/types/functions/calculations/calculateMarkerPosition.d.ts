import { CalculateMarkerPositionArgs } from "./types";
declare const calculateMarkerPosition: ({ position, parkCode, sectorCode }: CalculateMarkerPositionArgs) => import("../../model").LatLng;
export default calculateMarkerPosition;
