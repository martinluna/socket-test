import { LatLng, ParkCodes } from "../../model";
export interface InitMapArgs {
    position?: LatLng;
    apiKey: string;
    containerId: string;
    parkCode: ParkCodes;
    sectorCode: string;
    name: string;
}
